export enum PosteriorFilterEnumToKor {
  'participation' = '기획참여순',
  'latest' = '최신순',
  'oldest' = '오래된순',
}

export enum PosteriorFilterEnumToEng {
  '기획참여순' = 'participation',
  '최신순' = 'latest',
  '오래된순' = 'oldest',
}
